{
  "name": "danggeun-clone-step2",
  "private": true,
  "version": "1.0.0",
  "description": "당근마켓 클론 (step2) - 프론트·백엔드 래핑",
  "scripts": {
    "install:all": "npm install && npm run install --prefix backend && npm run install --prefix frontend",
    "dev": "concurrently -n be,fe -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "test": "npm run test --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
