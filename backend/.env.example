# Server
PORT=3001
NODE_ENV=development

# MySQL (danggeun)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=1234
DB_NAME=danggeun

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Upload
# UPLOAD_DIR=uploads
# 이미지 URL에 사용. 프론트와 같은 origin이면 생략 가능 (기본: http://localhost:PORT)
# API_BASE_URL=http://localhost:3001

# Production: CORS 허용 origin (쉼표 구분). 비우면 교차 출처 차단
# CORS_ORIGIN=https://your-frontend.com
