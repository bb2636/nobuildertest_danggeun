# API·소켓 기준 URL (백엔드 주소)
# 빈 값: Vite 프록시 사용 → /api, /socket.io 요청이 localhost:3001 로 전달됨 (백엔드 실행 필수)
# 직접 연결 시: http://localhost:3001
# ※ ERR_CONNECTION_REFUSED 나오면 백엔드 실행 여부 확인: cd backend && npm run dev
VITE_API_URL=
